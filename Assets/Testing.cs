using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour
{
    //public Transform displayDamageTransform;
    // Start is called before the first frame update
    void Start()
    {
        //Transform damageTransform = Instantiate(displayDamageTransform, Vector3.zero, Quaternion.identity);

        //DisplayDamage displayDamage = damageTransform.GetComponent<DisplayDamage>();
        //displayDamage.Setup(300);
        //DisplayDamage.Create(Vector3.zero, 300);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
